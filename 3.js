function three(a,b) {
    a = prompt("Task 3. Введіть імʼя:");
    b = prompt ("Введіть пароль:")
    if (a === "ivan" && b === "333ssss" ) {
       alert("Ласкаво просимо");
      } else {
        alert("Користувач не знайдений");
      }
  }
  three();
